# Inbox

Routed proposals awaiting triage. See `~/projects/claude-code-config/docs/routing.md`.

- Routed from dpsd-new (2026-09-22, minis, erga v0.5.0 upgrade, local builds with `OPENALEX_API_KEY`): **the two verdicts owed on the declared home, measured on the nine-author cohort.** Setup: `uvx erga==0.5.0 build` twice on the same config and overrides, once with top-level `home: https://ror.org/03zsp3p94` and once without, minutes apart, plus a third run on `erga==0.3.0` the same hour as a version control. All three wrote byte-identical `publications.json` (626 works), so v0.4/v0.5 changed the log and nothing else on this consumer. (1) **The Malisova cluster is reported whole.** Both runs: `Katerina Malisova: 5 work(s) tie to Palacký University Olomouc (CZ)`. Her raw profile (A5045528045, 16 works, 15 placed, GR 10 / CZ 5) carries exactly five works whose own authorship names Palacký, so 5 is the whole cluster. The consumer's overrides hold four DOIs; the fifth flagged work, `10.21203/rs.3.rs-3834098/v1`, is the 2024 Research Square preprint of the excluded BMC Public Health paper and is absent from the built JSON, so it folds into its published version before the overrides apply (read off the output, not the code). Worth a sentence in the warning's docs: the count is taken on raw works, so it can exceed the maintainer's override list while nothing is live; a maintainer reconciling 5 against 4 lines sees a gap that is not one. (2) **The declaration lifted no silence and produced a wrong-profile verdict on a correct profile under a correct declaration.** The one line that changed: Georgios Koronis. Undeclared, the majority rule inferred SG (own-authorship institutions: SUTD 16, Aegean 6, three Portuguese institutions 7, one US; 27 placed) and reported `2 work(s) tie to University of the Aegean (GR)` as a stranger cluster, i.e. his works at his actual employer. Declared: `21 of 27 placed work(s) sit outside GR, the declared home — either the declaration is wrong or this profile is not only theirs`. Neither holds: his CV has Lisbon, SUTD 2016-22 and Aegean since; one person, one profile. So the recent mover is the real-data case the pinned cohorts could not show (the one wrong-profile verdict there came from a minority-country modal institution, a proxy artefact); here the declaration is right and the verdict is still wrong, by construction, for anyone whose recorded career is mostly at a previous employer, and a department cohort will always contain some. The per-author opt-out `home: null` is the only remedy and it removes the check for that person. Two things for erga's judgment, neither a verdict: a declaration could carry a start year, or several homes, so a mover's earlier employers count as home rather than away; or the verdict text could name the third reading, "or they moved recently". The other two clusters, Gavalas at Essex (2, 1999-2003) and Papanikos at Toronto (7, 1994-99, his M.A.Sc. and PhD years per CV), are own early careers, identical in both runs, the accepted price. Also noted, not erga's doing: 303 of 626 works changed `authors` between the 09-21 CI build and 09-22 (OpenAlex expanded initials, `P. Papanikos` → `Paraskevas Papanikos`); the 0.3.0 control reproduces it, so it is index drift, not the upgrade. Consumer side recorded in dpsd-new `docs/publications.md` → Identity and editorial policy. Status: consumer #2 runs v0.5.0 from 2026-09-22 (both pins, `belalik/erga@v0.5.0` and `version: "0.5.0"`), with the department-wide `home:` declared and no per-author override or opt-out; `uvx erga==0.5.0` resolved from PyPI first time, and each build took about 2m15s locally. Vocabulary seen in the wild, still unmapped after the v0.4 `other` fix: `conference-abstract` (1 work), `peer-review` (2), `reference-entry` (10), each warning on every build and falling back to `other`. The reference entries are eight Encyclopedia of Computer Graphics and Games entries (four topics, each in the 2018-19 living edition and the 2024 print edition) plus one IGI handbook chapter under two DOIs; the peer reviews are the two author responses this consumer already excludes by override, so the warning fires on raw works before curation, like the cluster count above. Not asking for any change here.
