# Inbox

Routed proposals awaiting triage. See `~/projects/claude-code-config/docs/routing.md`.

- Routed from claude-code-config (2026-08-10): the global CLAUDE.md was restructured — its Gotchas section is now capped at 8 entries / ~400 words, and seven rules failed the hub's breadth test (domain knowledge — hosting provider, CI system, deploy stack — is project-local by default, however universal it felt at discovery) and were evicted to the projects that actually use them. Two apply here, both GitHub-CI facts, and this project is pre-0.1 with its GitHub Action not yet validated live, so they land before the mistakes rather than after. **The trigger is the eviction itself:** each rule is now held by no session until the receiving project adopts it. These are *avoidance* gotchas — an on-demand `docs/` page is a poor home, since it only gets opened after the mistake; this project's CLAUDE.md is the intended destination. Prior wording verbatim: `git show 1668164:user-memory/CLAUDE.md`. The two:
  - **GitHub Actions:** a push authenticated with the default `GITHUB_TOKEN` never triggers another workflow. A bot commit expecting a downstream `on: push` build must dispatch it explicitly (`actions: write` + `gh workflow run`). Relevant if the publications pipeline ever commits `publications.json` and expects a consuming site to rebuild.
  - **Fine-grained PATs** are scoped to exactly ONE resource owner (user or org) — they 404 on any other org's repos regardless of permissions, indistinguishable from "file not found". A local `gh` token usually has broader access, so hand-testing masks it; verify in CI. Transferred repos still resolve via GitHub's redirect, so the old path working proves nothing about the token. Additional case routed from dpsd-new and never applied globally: a PAT requesting an org you are only a `member` of needs owner approval and 404s *while pending*, identically to no-access and no-file — the sound check is `GET /user/memberships/orgs/{org}` → `.role`. Sharp here, since erga is designed to write into *other* people's repos across owners.
